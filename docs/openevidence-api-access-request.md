# OpenEvidence — Programmatic Access Request (draft)

Send via OpenEvidence's support/contact channel (in-app **Help → Contact**, or
support@openevidence.com), or to a specific contact if you have one. Replace the
bracketed fields before sending.

---

**Subject:** Request for sanctioned programmatic / API access — verified physician account

Hello OpenEvidence team,

I'm a verified physician on OpenEvidence ([account email: **ppoiu87@gmail.com**],
HCP-verified, Internal Medicine, Taiwan). I use OpenEvidence for point-of-care
clinical lookups and would like to access it programmatically for my own
workflow rather than through the web UI.

**What I'd like:** a sanctioned way to submit clinical questions and retrieve
answers/citations programmatically — for example an official API, an API key,
or an allowlisted integration tied to my verified account. I want to do this
within your terms of service, not around them.

**Why:** I'd like to integrate OpenEvidence into my personal clinical-reference
tooling (a local assistant that organizes my own past queries and collections).
Today the web endpoints sit behind bot protection (DataDome), which correctly
blocks my automated client — so I'm reaching out for the proper, supported path
instead of trying to work around that protection.

**Scope / volume:** single-user, low volume (well under typical human usage —
on the order of a few dozen queries per day, rate-limited on my side). Personal
use only; no resale, scraping, or redistribution of content.

**Happy to provide:** account verification, agreement to API/usage terms, a BAA
if required, and any technical details about my integration.

Is programmatic access available for individual verified clinicians, and if so,
what's the process? If it's enterprise-only or not currently offered, I'd
appreciate knowing that as well.

Thank you,
[**Dr. Hsieh-Ting Lin / 林協霆**]
[contact email / phone]

---

## Notes for you

- **Lead with verified-physician status + within-ToS framing.** That's what
  separates this from a scraping request.
- **Be honest about the DataDome block.** It signals you respect their
  protection rather than evading it — which is the whole point.
- **Keep volume small and concrete.** Single-user, rate-limited, personal.
- If they say no / enterprise-only, the realistic fallback is to keep using
  OpenEvidence through the **web UI** for `ask`, and reserve the MCP for the
  read-path tools (history, collections, article retrieval) that already work.
